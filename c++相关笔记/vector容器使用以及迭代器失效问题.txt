vector容器迭代器失效问题:
**当我在一个vector容器中插入一组数之后,iterate的位置会发生变化么?(insert中间的迭代器)
    迭代器的第一个参数是const_iterate _where(并不是引用或者指针,值不变,所以它指的位置是不变的)
**当删除一个元素时(erase)
    报错! 迭代器被删除了(失效了),要想使用这个迭代器则需要一个变量去接这个返回值(iv = v.erase(iv))
**insert end处的迭代器导致增容后迭代器还指向原来的空间,而原来的空间已经被释放了,会导致原来的迭代器失效
    用一个iv去接返回插入的点的返回值
